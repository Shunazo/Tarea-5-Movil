import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Psj3Page } from './psj3.page';

describe('Psj3Page', () => {
  let component: Psj3Page;
  let fixture: ComponentFixture<Psj3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Psj3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
